//

// solving:
// from: 

#include <vector>
#include <iostream>

int main() {
    int n;
    std::cin >> n;

    String s;
    std::cin >> s;

    // If there are no unique letters, take the letter with the fewest occurences to be "unique" and take only one of them.

    return 0;
}